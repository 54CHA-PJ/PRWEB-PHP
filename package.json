{
  "dependencies": {
    "sass": "^1.71.1"
  },
  "scripts": {
    "sass": "sass --watch scss:css",
    "sync": "browser-sync start --proxy localhost/PRWEB --files '**/*.php, **/*.html, **/*.css, **/*.js'"
  }
}